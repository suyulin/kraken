import 'package:flutter/foundation.dart';

class Config {
  static const bool allowComment = true;
  static const bool debugPerformance = !kReleaseMode && false ;
  static const bool enableDebugPrint = false ;
  static const bool enableMethodChannel = false ;
  static const bool enableEmit = false ;
}
